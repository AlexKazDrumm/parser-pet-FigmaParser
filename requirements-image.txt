# Optional: adds HTML rasterisation and diff metrics to /api/image/export.
# After installing, download the browser once:  python -m playwright install chromium
-r requirements-ai.txt
Pillow==12.3.0
playwright==1.56.0
